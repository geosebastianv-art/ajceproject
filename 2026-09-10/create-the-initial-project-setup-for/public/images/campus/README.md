# Campus image assets

This folder is reserved for approved AJCE campus and facilities photography.
Until those assets are supplied, the facility data in `lib/site-config.js` reuses the
temporary development image already used elsewhere on the landing page.
