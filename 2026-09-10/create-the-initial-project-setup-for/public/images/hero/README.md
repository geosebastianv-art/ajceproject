# Hero image asset

Replace the temporary external development image configured in `lib/site-config.js`
with an approved AJCE photograph saved in this folder. Keep the final filename and
the `heroImageUrl` value aligned (for example, `/images/hero/ajce-campus.jpg`).
